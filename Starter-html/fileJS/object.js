// Object

const person = {
    name: 'John'
    age: 30,
    weight: 88.6,
    isAdmin: true
}

// Ex: 1
console.log(person.name) // pegar a propriedade dentro do objeto

// Ex: 2
console.log('${person.name} tem ${person.age} anos')